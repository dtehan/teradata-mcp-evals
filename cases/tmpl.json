{
  "module": "tmpl",
  "cases": [
    {
      "_comment": "Happy path cases are auto-generated by generate_cases.py — run it first, then add ambiguous_selection, missing_parameter, and multi_tool cases here by hand.",
      "_instructions": "After running: uv run python generate_cases.py --module tmpl, edit this file to add edge case types."
    }
  ]
}
